version: '3.8'

services:
  build:
    image: neighborville-build
    build:
      context: .
      dockerfile: Dockerfile
      target: build
    volumes:
      - build_output:/app/dist

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    restart: always 
    volumes:
      - build_output:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - build

volumes:
  build_output: